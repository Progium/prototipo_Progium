'use strict';

/**
 * MenuClienteController
 * @constructor
 */
var MenuClienteController = function($scope, $rootScope, $http, $window) {
  $scope.mostrarMensaje = false;
  $scope.esVisible = true;

  // $scope.usuario = {};

  // $scope.init = function() {
  //    $scope.usuario.nombre = "Admin",
  //    $scope.usuario.contrasenna = "1234"
  // };

  // $scope.init();

};